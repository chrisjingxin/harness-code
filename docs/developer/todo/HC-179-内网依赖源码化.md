# HC-179 内网依赖源码化执行清单

关联 Plan：[HC-179-内网依赖源码化](../plan/HC-179-内网依赖源码化.md)。本清单执行到“分支可搬运”停点；npm 迁移阶段执行到“npm 安装链路可搬运”停点。

## npm 包管理器迁移（范围变更批次）

- [x] N1 先运行 `bun test scripts/dependencies`，确认 npm 契约测试缺失时失败。
- [x] N1 `source_policy.test.mjs` 增加 `validateNpmLockSource`（package-lock.json fixture：内网 URL 通过、公网 URL/缺文件失败）与 npm/node 版本校验用例；旧 Bun 前置用例已在 B3 随职责移除。
- [x] N1 `vendor_policy.test.mjs` lock fixture 换为 package-lock.json 结构：五包 `link: true` 通过；registry 解析、嵌套 locator 失败。
- [x] N2 `source_policy.mjs`：`validateBunLockSource` → `validateNpmLockSource`；新增 `expectedNpmVersion` 与 node 最低版本校验。
- [x] N2 `vendor_policy.mjs`：`validateVendoredWorkspaceLock` 解析 package-lock.json 的 packages 树。
- [x] N3 `install.mjs`：freeze → `npm ci --registry <内网源> --legacy-peer-deps`；resolve → `npm install --registry <内网源> --legacy-peer-deps`；preflight 校验 npm/node/uv/Python。
- [x] N3 `check_dependencies.py`：`--bun-lock` → `--npm-lock`，适配 package-lock.json 结构；`packages/agent/tests/test_dependency_check.py` 同步。
- [x] N4 根 `package.json`：`packageManager` 改 npm、`engines` 增加 bun/node；`test:web:e2e` 的 `bunx` → `npx`。
- [x] N4 workspace 依赖声明适配 npm：CLI 与五包间 7 处 `workspace:*` 改精确版本（`@za38/protocol` 用 `*`）；vendored `@opentui/core-win32-x64` 清单移除 `os`/`cpu`。
- [x] N4 `vendor_policy.mjs` 门禁同步：workspace 边改为精确版本断言、禁止 `workspace:` 残留、win32-x64 的 os/cpu 反向断言、`lockJson` 严格解析；`vendor_policy.test.mjs` 同步。
- [x] N4 `provenance.json`：移除三个 workspace 归一化条目、新增 os/cpu 移除记录、更新三包 `directorySha256`。
- [x] N4 新增根 `.npmrc`（`legacy-peer-deps=true`）；干净临时副本中生成平台全量基准 `package-lock.json`；删除 `bun.lock`。
- [x] N5 更新 README、内网依赖安装.md、架构总览.md、Task front matter 中 `bun install`/`bun.lock` 表述。
- [x] N5 运行 `bun run deps:test`、`bun run typecheck`、`bun run project:check`、`bun run docs:check`；确认 darwin 上 `deps:install` 仍被平台门禁拒绝。
- [x] N5 写入中文 `tmp/handoff.md`，停在 npm 链路可搬运停点等待用户验收；内网 `deps:resolve`/空缓存安装/TUI 属内网验收。

## npm 统一命令入口（范围变更批次）

- [x] N6 RED：增加 manifest 契约测试，证明根与自有 workspace scripts 当前仍含 `bun run`。
- [x] N6 GREEN：根脚本编排改为 `npm run` / `npm --workspace … run …`；直接 Bun 边界改为 `bun <script>`，保留 `bun test`、`bun build`、CLI/TUI 与 FFI。
- [x] N6 文档：依赖安装器提示、README、快速开始、故障排查、内网依赖安装说明和架构命令示例统一为 `npm run`。
- [x] N6 验证：manifest test 与 `npm run deps:test` 28 passed；`npm run build`、`npm run project:check`、`npm run docs:check`、`git diff --check` 通过；`npm run deps:install` 在 darwin/arm64 于联网前按预期拒绝；`npm run typecheck` 与 `npm run dev -- --version` 因当前工作树尚未安装 `@types/bun` / `@opentui/core` 失败，未误判为命令迁移缺陷。
- [x] N6 更新中文 `tmp/handoff.md`，停在“npm 工程命令入口”可演示点等待用户查看。
- [x] N7 审计 Unix/PowerShell 全局安装器：仓库尚未交付 `scripts/install/` 或对应脚本，本任务记录不适用且不创建安装器脚手架。
- [x] N7 审计 AGENTS 与剩余活动文档，更新验证证据并再次停点（并入停点 B E4）。

## 依赖工具链零 Bun（已完成）

- [x] B1 RED：用 Node 内置测试锁定 `engines.bun = 1.3.13`，并证明 `deps:install`、`deps:resolve`、`deps:check`、`deps:test`、`test:py` 当前仍会启动 Bun。
- [x] B2 把 `source_policy`、`vendor_policy` 与现有依赖测试迁移为原生 `.mjs` + `node:test`，不新增依赖。
- [x] B3 把 `install`、`check_vendored_packages`、`run_python` 迁移为原生 `.mjs` + `node:child_process`；删除依赖安装链路中的 Bun 版本检查。
- [x] B3 根 scripts 改为 Node/npm/Python 调用，五个依赖命令的完整调用链不再出现 Bun。
- [x] B4 将 Bun 运行时精确固定为 `1.3.13`，更新当前 README、用户说明、架构与内网安装说明；历史证据保持原样。
- [x] B4 运行 focused tests、`npm run deps:test`、本机可达的依赖命令、假 Bun PATH 回归和 `git diff --check`；更新中文 `tmp/handoff.md` 后停下等待验收。
- [x] B5 RED：证明没有 `HARNESS_*` 时可使用 npm/pip 已配置内网源，并证明工具配置优先于备用变量。
- [x] B5 GREEN：安装入口读取 npm/pip 当前有效配置，把 `HARNESS_NPM_REGISTRY` / `HARNESS_PYPI_INDEX` 降为备用，同时保持所有内网来源门禁。
- [x] B5 更新当前使用文档、运行依赖测试与项目检查，并更新中文 `tmp/handoff.md`。
- [x] B6 RED：manifest 契约要求唯一安装入口为 `deps:sync`，旧 `deps:install` / `deps:resolve` 不再存在。
- [x] B6 GREEN：`install.mjs` → `sync.mjs`；默认冻结同步，`--update-lock` 更新锁后自动完成完整同步与验证。
- [x] B6 更新当前文档、运行两种模式门禁、依赖测试与项目检查，并更新中文 `tmp/handoff.md`。

## 停点 B：工程工具零 Bun（已完成）

- [x] E1 RED：manifest 契约逐项拒绝 Protocol、Task、文档、版本、发布、项目检查和工程测试调用链中的 Bun。
- [x] E2 新增受限 Node TypeScript loader；迁移工程工具的 Bun 专属 `import.meta` 用法和相对导入。
- [x] E2 把项目工程测试从 `bun:test` 迁移为 `node:test` / `node:assert/strict`，保持全部行为用例。
- [x] E3 把 `packages/protocol` 与根工程 scripts 切到 Node/npm，更新生成器错误提示和任务看板命令文案。
- [x] E4 更新 AGENTS 与活动工程文档，运行工程/Protocol/依赖检查和失败假 Bun PATH 回归，更新中文 `tmp/handoff.md` 后停点。

## 停点 C：Bun 收敛到 CLI 孤岛（已完成）

- [x] C1 RED：manifest 契约证明根 `dev` 和 CLI `syntax:assets` 仍直接执行 Bun，并锁定最终允许的 Bun scripts 集合。
- [x] C2 根 `dev` 改为 npm workspace 转发且保留参数边界；语法资源维护脚本迁移为原生 Node ESM。
- [x] C2 更新 CLI 相邻架构测试与生成文件来源注释；保留 OpenTUI/FFI、Bun bundler 和 CLI 测试边界。
- [x] C3 审计端侧安装器并记录不适用；更新活动用户、工程与架构文档中的 canonical 命令和必要性说明。
- [x] C4 运行 focused tests、Node 语法检查、CLI 相邻测试、依赖/项目检查与 Bun 边界审计；更新证据和中文 `tmp/handoff.md` 后停点。

## 源跟随本机配置

- [x] 企业外的公网 npm/pip 配置和未配置时的公网默认源可以通过 `deps:sync`；企业内配置或 `HARNESS_*` 仍切到内网，且内网模式拒绝公网下载地址。
- [x] `source_policy.test.mjs` 覆盖公网默认源、已提交公网锁、内网覆盖公网配置，以及内网 npm 不搭配公网 Python。

## 停点 D：跨平台依赖同步与 npm 验证

- [x] D1 RED：平台契约覆盖 macOS/Windows/Linux 的 x64/arm64、Linux glibc/musl，以及不支持目标的失败关闭。
- [x] D1 RED：fixture 补齐八个 OpenTUI native optional 包，覆盖本机 native 包缺失、错误身份/平台、缺入口和缺原生库。
- [x] D2 GREEN：解除 `deps:sync` 的 Windows-only 门禁，静态校验完整 native optional/lock 矩阵。
- [x] D2 GREEN：安装后按当前平台与 `OPENTUI_LIBC` 校验 native 包；`deps:sync` 与 `deps:check` 共用门禁。
- [x] D3 更新用户、工程与架构文档，说明各平台来源、验证范围及 CLI Bun 边界。
- [x] D3 在当前 macOS 运行 focused tests、`npm run deps:test`、`npm run test:project`、`npm run project:check`、`npm run deps:sync` 可达门禁与 `git diff --check`；更新证据和中文 `tmp/handoff.md` 后停点。

## 前置与任务链

- [x] 核对 `AGENTS.md`、Git 状态和基线提交 `0bff89e6cd9cf3e326db68d90a39a4f8e1e88885`；确认工作区干净后创建 `feat_hc_179_内网依赖源码化`。
- [x] 用 `bun run task:claim -- HC-179 --owner Codex --branch feat_hc_179_内网依赖源码化` 写入认领信息并同步看板。
- [x] 创建同名 Task / Spec / Plan / Todo，写明临时分支退出条件和未运行的目标环境验收。

## TDD：源码化门禁

- [x] 先运行 `bun test scripts/dependencies/vendor_policy.test.ts`，确认实现缺失时失败。
- [x] 增加正常 fixture，以及错误版本、文件篡改、缺失发布入口/DLL、错误执行平台、错误包平台、registry 回退、嵌套 lock locator、重复补丁和安装后 realpath 用例。
- [x] 实现 `scripts/dependencies/vendor_policy.mjs`：版本、MIT license 元数据、tarball integrity、目录摘要、显式入口、provenance、Windows x64、workspace 边、安装后 realpath 和 lock fail-closed 检查。
- [x] 增加 `scripts/dependencies/check_vendored_packages.mjs`，接入 `deps:check`；在 `install.mjs` 的联网动作前接入静态门禁，并在 npm 安装后接入实际解析门禁。
- [x] 保留并复核 `packages/agent/tests/test_dependency_check.py` 的 Anthropic 禁止包回归；不得移除 DeepAgents 安装补丁或 Python 路径检查。

## 精确发布工件与 workspace

- [x] 从精确 npm tarball 解包五个包，并用基线 `bun.lock` 的 SHA-512 校验原始制品。
- [x] 保留发布目录内的 JS、声明、资源、README、已有许可证和 `bun-ffi-structs` / `react-devtools-core` 的完整 `dist/**`；不提交 tarball、node_modules、cache。
- [x] 只做必要的 package.json workspace/dev metadata 归一化；将 `react-devtools-core` 的 `ws` 固定为 `7.5.10`，保留根 patch 文件作为来源证据。
- [x] 更新根 workspace、CLI 直接依赖、五包运行时边和 `bun.lock`；扫描全部 locator，验证五个包不存在 registry 解析。
- [x] 生成并校验 `third_party/npm/provenance.json`，包含目标平台、integrity、许可证、目录 hash、patch hash 和归一化记录。

## 文档与可搬运停点

- [x] 更新 README、`docs/user/快速开始.md`、`docs/developer/project/内网依赖安装.md` 与 `docs/developer/architecture/架构总览.md`。
- [x] 运行 `bun run deps:test`；完成信号：源码策略与源码化状态机全部通过（24 tests / 31 expectations）。
- [x] 运行 `bun run typecheck`、`bun run build`、`bun run project:check`、`bun run docs:check`、`git diff --check`，全部通过。
- [x] 尝试 `bun run deps:check`；源码化静态与安装后 realpath gate 通过，整条命令因本机没有 `packages/agent/.venv` 中的 Python 阻断；`deps:install` 在当前 darwin/x64 于联网前被平台门禁阻断，未绕过门禁。
- [x] 从当前提交创建全新 Git snapshot，删除所有 ignored 安装物后复跑静态 vendor gate，确认提交树包含两个包的完整 `dist/**` 和所有声明入口。
- [x] 写入中文 `tmp/handoff.md`，记录分支、提交、文件/体积、已运行与未运行检查；停下等待用户验收。

## 本任务明确不做

- [x] 不 vendor 第六个 npm/Python 依赖，不提交 tarball、node_modules 或缓存。
- [x] 不从当前 macOS node_modules 复制 OpenTUI，不改 OpenTUI import、JSX 或 native loader。
- [x] 不执行或声称完成 Windows x64 空缓存安装、TUI 验收和浏览器 E2E。
- [x] 不 push、不合并 master、不归档 Task；正式内网依赖可用后的退出清理属于后续工作。
