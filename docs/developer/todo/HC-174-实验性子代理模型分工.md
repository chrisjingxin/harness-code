# HC-174 实验性子代理模型分工执行清单

关联：[Task](../task/HC-174-实验性子代理模型分工.md) · [Spec](../spec/HC-174-实验性子代理模型分工.md) · [Plan](../plan/HC-174-实验性子代理模型分工.md)

状态：未执行。2026-09-10 已与 Task/Spec/Plan 及当前源码事实拉齐。所有 checkbox 仅在动作和验证完成后勾选；V1～V9 的完整命令见 Plan 第 6 节（`rtk …`，不用 `rtk proxy`）。每次只推进到下一个可演示停点；不得为了勾完清单一次完成全部功能。

## 执行前

- [ ] 读取当前 Task/Spec/Plan、相邻 HC-161/169 状态和 `git status --short`；工作区若已有未提交改动（含 `runtime/prompts/system_prompt.md`）一律保留，不回滚。完成信号：交接记录列明基线与已有改动。
- [ ] 按仓库规则建立 `feat_hc_174_实验性子代理模型分工`（可截短）并通过 `task:claim` 认领；当前文档分支名不代替认领。记录 HC-131 过期检查阻塞，未解决时不篡改他人日期。完成信号：Task 的 owner/branch 与实际实施一致。
- [ ] 使用 `test-driven-development`，准备隔离文件 fixture 与两个 mock Profile；禁止真实凭据、网络模型实验和修改用户配置。完成信号：mock 能区分 root/child 请求，测试不需要真实模型。

## 停点 A：只读助手使用指定模型

依赖：执行前完成。对应 Plan A1～A4。

- [ ] **A1a 配置契约**：把 `[experimental]` 登记进 ConfigManifest.SECTIONS（USER/EXPLICIT）；在 config.py 与 test_config.py 添加缺省/false/true、两角色字段、未知子表/非法类型/来源/引用测试，再实现解析与 `redacted()` 脱敏显示。不加入 ConfigChangeService 白名单。验证 V1，关闭不解析闲置引用，开启无有效角色明确失败。
- [ ] **A1b 受理预检**：接入 Host 的 Run 受理，检查绑定模型能力与凭据可用性，禁止静默回退；验证 V1，失败时无 root/child 模型请求，重启前不热换活动绑定。A 阶段尚未交付的执行角色明确拒绝运行，不装作已支持。
- [ ] **A2a 角色 spec**：在 agent_spec.py/builtin_agents.py 复用角色上限并绑定指定 Profile，新增相邻测试；验证 V2，未绑定和 Plugin 沿原规则、绑定与主模型同名仍选 Managed。
- [ ] **A2b 唯一目标与池身份**：在 engine profile 与目标装配中纳入有效策略指纹，确保一个 ID 一个 target；验证 V2，绑定变化不复用旧图，关闭时闲置字段不改变有效身份。
- [ ] **A3a Managed 接线**：修改 agent_host.py/agent.py 复用 Managed executor，填写 `engine_profile_key`；不复制 Plugin runner，不给内建角色挂 Plugin Agent Hook。explore 不继承父 MCP/Skill；若本段误接到 GP，不得丢掉父 MCP/Skill。验证 V2/V3，mock pro 派发、mock fast 调查、父收到文本并继续，lease/checkpoint 独立。
- [ ] **A3b 只读安全**：在相邻 child/Managed 测试覆盖工具求交、父权限收紧、Plan、禁写/禁 Shell/禁再委派和池容量耗尽；验证 V3，无池外 engine 或权限扩张。
- [ ] **A4a 文本与隔离**：在角色条件提示中加入简洁结论/证据/疑点要求；测试普通文本、Markdown、未完成说明及标记原文；验证 V2/V3，父历史不自动含 child 工具过程，不新增 JSON/partial/长度规则，摘要不能授予 Snapshot。
- [ ] **A4b 大结果回归**：用真实 task middleware 链覆盖阈值以上结果与外存失败，并验证父可主动读取引用；验证 V3，行为与现有普通子代理一致，无复制阈值或实验专用错误码。
- [ ] **A4c 错误与取消**：补已识别 provider 失败、超时、父取消竞态、slot/lease 回收用例；验证 V3，已知工具失败父可继续，取消原样传播，权限/身份/未知错误 fail closed。
- [ ] **A4d 可见模型**：沿现有 child 元数据/卡片展示实际 Profile 和运行状态；`/agents` 对已绑定 explore 显示该 Profile。建立可回放 mock 事件 fixture；验证 V4，TUI/Web 一致、模型不由正文猜测、未宣称完整总账。
- [ ] **A 停点交付**：运行本段 focused tests、相关包检查；更新用户说明的已交付范围。用 mock fixture 展示“explore / fast / 最终文本 / 父继续”，用户自行启用后可在 `rtk bun run dev` 查看相同卡片；在 Task 与 tmp/handoff.md 写证据，**停下等用户查看，未经要求不做 B**。

## 停点 B：完整用量展示

依赖：停点 A 用户已查看并要求继续。对应 Plan B1～B3。

- [ ] **B1a 调用身份**：在现有 model lifecycle/execution_stream.py 按 execution/Profile/round/attempt 记录调用；验证 V5，分片与最终消息/重复事件只计一次，10+20=30，不取 session 最大值。
- [ ] **B1b 失败与未知**：补重试、失败/取消已知用量、缓存/推理明细、缺失字段及零调用用例；验证 V5，内容回滚不回滚消耗，缺失不是零，不对缓存/推理重复相加。
- [ ] **B2a 子执行自身用量**：从 Inline/Managed 经 delegator 传到 execution registry；验证 V3/V5，实验 Run 内未绑定角色与 Plugin 不遗漏；辅助调用未覆盖明确使摘要不完整。
- [ ] **B2b Run 终态汇总**：在 run_coordinator.py 等现有终态路径汇总自身与所有后代；验证 V5/V6，root=30/child=7/total=37，失败/取消也保留数据，墙钟不相加，Registry 丢弃前完成摘要。
- [ ] **B3a 协议与历史**：在 v3.json 为 completed/failed/cancelled 增加可选 `delegation_usage`（这三份 payload 目前 `additionalProperties: false`，cancelled/failed 现无 usage）。生成 Python/TS，接既有持久化；验证 V6/V8，旧整数 `usage` 不变、关闭无新摘要，旧记录不补造统计。
- [ ] **B3b 诊断同步**：同步既有日志契约与生成物，只记录脱敏模型/执行身份/用量及完整性；验证 diagnostic_log/test_contract.py 与 test_execution_context.py，无正文或秘密。
- [ ] **B3c 两端展示**：shared state/selector 消费唯一摘要，TUI/Web 展示自身/合计、child 模型/耗时、未知/至少 N；验证 V4/V7，实时与历史回放一致，无前端重复归账或货币节省估算。
- [ ] **B 停点交付**：运行 V5/V6/V7/V8，更新统计用户说明；用 fixture 展示成功/失败/取消与未知四种摘要，刷新仍 root=30/total=37。记录 Task 与 tmp/handoff.md，**停下等用户查看，未经要求不做 C**。

## 停点 C：可选执行助手与完整验收

依赖：停点 B 用户已查看并要求继续。对应 Plan C1～C3。

- [ ] **C1 执行角色接线**：沿同一角色 spec/Managed 装配启用 general-purpose，移除 A 阶段未交付拒绝；验证 V2/V3，单独配置 explore 不影响执行角色，额外绑定后 mock fast 经 canonical 工具修改隔离文件并返回普通文本。GP 仍继承父 MCP/Skill 与 auto-edit 例外，不挂 Plugin Agent Hook。
- [ ] **C2a 审批与恢复**：覆盖父权限动态收紧、Plan、目录信任、Snapshot、并发冲突与审批恢复；验证 V3/V9，权限不扩大、同一 child checkpoint 恢复且不重复提交修改。
- [ ] **C2b 部分修改失败**：模拟一次成功修改后 provider 失败、超时或取消；验证 V3/V6/V9，timeline 保留已发生操作、提示可能部分变更，无自动回滚/换模型重跑，总账保留已知消耗。
- [ ] **C3a 用户与架构文档**：更新模型配置、交互说明、subagent、默认关闭示例，以及配置架构与架构总览；核对关闭方式、重启生效、与 `[models.roles]` 的区别、实验限制与普通文本复用。验证 docs/project 检查；不新增架构平行文档或未授权范围。
- [ ] **C3b 完整验证**：执行 V8、`rtk bun run test`、`rtk bun run build`，按 Task 十项验收填证据；环境跳过和 HC-131 等既有阻塞如实记录，不写全通过。
- [ ] **C3c Review**：使用 code-review-and-quality 对照 Task/Spec 与 diff 检查模型绑定、权限、计量、取消、历史和关闭回归，修复发现问题并跑受影响测试；完成信号：review 结论及修复证据写入 Task。
- [ ] **C 停点交付**：用隔离 fixture 展示“general-purpose / fast / 修改 / 验证摘要 / 用量”，再演示修改后失败和关闭回归；更新 tmp/handoff.md，**停下等待用户验收，不自动提交或发布**。
- [ ] **完成与归档**：仅在全部实现、验证、review、文档和验收条件满足后，记录版本影响与实际提交/PR 情况，通过仓库 task complete 流程归档、同步看板并重跑 project:check；有未解决检查阻塞不得标已完成。

## 冻结的非范围

- 不新增实验 JSON、12 KiB 上限、强制字段/段落、partial 交付状态或格式修复调用。
- 不做真实模型效果对比、固定节省承诺、Portal/Mode 平台或旁路脚本。
- 不做读取强制拦截、自动升级、递归委派、通用输出契约平台。
- 不改 Compose/Team 调度，不把内建角色做成 Plugin Agent，不把 `[models.roles]` 当第二入口。
- 不改真实用户配置，不覆盖用户现有修改，不为通过检查刷新其他任务日期。
- 发现需要改变公开行为、范围或生命周期时，回上游文档修订；不得在 Todo 执行中发明新设计。
