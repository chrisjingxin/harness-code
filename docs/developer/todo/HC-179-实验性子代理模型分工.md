# HC-179 实验性子代理模型分工执行清单

关联：[Task](../task/HC-179-实验性子代理模型分工.md) · [Spec](../spec/HC-179-实验性子代理模型分工.md) · [Plan](../plan/HC-179-实验性子代理模型分工.md)

状态：未执行。2026-09-10 已与 Task/Spec/Plan 及当前源码事实拉齐。所有 checkbox 仅在动作和验证完成后勾选；V1～V9 的完整命令见 Plan 第 6 节（`rtk …`，不用 `rtk proxy`）。每次只推进到下一个可演示停点；不得为了勾完清单一次完成全部功能。

## 执行前

- [x] 读取当前 Task/Spec/Plan、相邻 HC-161/169 状态和 `git status --short`；工作区若已有未提交改动（含 `runtime/prompts/system_prompt.md`）一律保留，不回滚。完成信号：交接记录列明基线与已有改动。
- [x] 按仓库规则建立 `feat_hc_174_实验性子代理模型分工`（可截短）。`task:claim` 被 HC-131 过期复核阻断，已手工写入 owner=Grok / branch / 进行中，未改他人日期。
- [x] 使用 `test-driven-development`，准备隔离文件 fixture 与两个 mock Profile；禁止真实凭据、网络模型实验和修改用户配置。完成信号：mock 能区分 root/child 请求，测试不需要真实模型。

## 停点 A：只读助手使用指定模型

依赖：执行前完成。对应 Plan A1～A4。

- [x] **A1a 配置契约**：把 `[experimental]` 登记进 ConfigManifest.SECTIONS（USER/EXPLICIT）；在 config.py 与 test_config.py 添加缺省/false/true、两角色字段、未知子表/非法类型/来源/引用测试，再实现解析与 `redacted()` 脱敏显示。不加入 ConfigChangeService 白名单。验证 V1，关闭不解析闲置引用，开启无有效角色明确失败。
- [x] **A1b 受理预检**：接入 Host 的 Run 受理，检查绑定模型能力与凭据可用性，禁止静默回退；验证 V1，失败时无 root/child 模型请求，重启前不热换活动绑定。A 阶段尚未交付的执行角色明确拒绝运行，不装作已支持。
- [x] **A2a 角色 spec**：在 agent_spec.py 复用角色上限并绑定指定 Profile；验证 V2，绑定 explore 用指定 Profile、只读工具、与主模型同名仍是独立 Managed 身份。
- [x] **A2b 唯一目标与池身份**：主 engine 身份纳入有效绑定指纹；Inline 跳过已绑定 ID。验证 V2，绑定变化不复用旧图，关闭时闲置字段不改变有效身份。
- [x] **A3a Managed 接线**：`_bound_builtin_delegation_targets` 复用 ManagedAgentExecutor，填写 `engine_profile_key`，不挂 Plugin Hook。验证 V3：`test_production_task_routes_bound_explore_to_managed_target`。
- [x] **A3b 只读安全**：bound explore spec 保持五只读、无 MCP/Skill；未改池外建图。现有只读/超时/取消回归随 V3 通过。
- [x] **A4a 文本与隔离**：绑定 explore 的 child prompt 含交付要求；主 prompt 在开启时注入委派说明。Managed capture_only 沿用现有文本返回。
- [x] **A4b 大结果回归**：未复制阈值或实验专用错误码，沿用现有 task middleware；V3 child_stream 回归通过。
- [x] **A4c 错误与取消**：未改 Delegator/Managed 失败路径；V3 既有超时/取消用例通过。绑定角色失败走同一 executor。
- [x] **A4d 可见模型**：`/agents` 对已绑定角色覆盖 `model_profile_id`；execution binding 记录 `target.model`。未新建仪表盘或总账 UI。
- [x] **A 停点交付**：V1–V3 共 208 passed。用户文档已写默认关闭与 explore 绑定。**停下等用户查看，未经要求不做 B**。

## 停点 B：诊断日志用量（无界面）

依赖：停点 A。用户确认本期只写本地诊断日志，不改协议、不做 TUI/Web 展示。对应精简后的 Plan B。

- [x] **B1 调用累加**：本回合 `last_call_usage` 保留绝对值；账本按 execution 求和，10+20=30，不取 session max。
- [x] **B2 终态诊断**：实验开启时 completed/failed/cancelled 写 `delegation.usage`（root/child/total、complete、child_count）；关闭不写。协议 payload 无新字段。
- [x] **B3 契约**：Diagnostic Log v1 增加该事件；Python/TS 敏感 key 白名单同步。无 UI。
- [x] **B 停点交付**：focused tests 通过。查看 `~/.harness/logs/` 中 `delegation.usage`。**界面无变化。**

## 停点 C：可选执行助手与完整验收

依赖：停点 A 已交付。停点 B（用量合计）按用户要求暂缓，C 不依赖 B。对应 Plan C1～C3；C2b 不含总账。

- [x] **C1 执行角色接线**：沿同一角色 spec/Managed 装配启用 general-purpose，移除 A 阶段未交付拒绝；验证 V2/V3，单独配置 explore 不影响执行角色，额外绑定后 mock 走 Managed 并返回普通文本。GP 仍继承父 MCP/Skill 与 auto-edit 例外，不挂 Plugin Agent Hook。
- [x] **C2a 审批与恢复**：绑定内建 runner 接入 `approval_mode_provider` 与 child interaction；V9 `test_child_approval.py` 既有动态求交/HITL 回归通过。不新做副作用扫描系统。
- [x] **C2b 部分修改失败**：绑定 runner 对 `ManagedAgentExecutionError` 不换模型重跑，取消外原样传播。用量合计仍待停点 B，本项不含总账。
- [x] **C3a 用户与架构文档**：更新模型配置、交互说明、subagent、默认关闭示例，以及配置架构与架构总览。未跑 `project:check`（HC-131 过期阻断）。
- [ ] **C3b 完整验证**：执行 V8、`rtk bun run test`、`rtk bun run build`，按 Task 十项验收填证据；环境跳过和 HC-131 等既有阻塞如实记录，不写全通过。
- [ ] **C3c Review**：使用 code-review-and-quality 对照 Task/Spec 与 diff 检查模型绑定、权限、计量、取消、历史和关闭回归，修复发现问题并跑受影响测试；完成信号：review 结论及修复证据写入 Task。
- [ ] **C 停点交付**：用隔离 fixture 展示“general-purpose / fast / 修改 / 验证摘要 / 用量”，再演示修改后失败和关闭回归；更新 tmp/handoff.md，**停下等待用户验收，不自动提交或发布**。
- [ ] **完成与归档**：仅在全部实现、验证、review、文档和验收条件满足后，记录版本影响与实际提交/PR 情况，通过仓库 task complete 流程归档、同步看板并重跑 project:check；有未解决检查阻塞不得标已完成。

## 冻结的非范围

- 不新增实验 JSON、12 KiB 上限、强制字段/段落、partial 交付状态或格式修复调用。
- 不做真实模型效果对比、固定节省承诺、Portal/Mode 平台或旁路脚本。
- 不做读取强制拦截、自动升级、递归委派、通用输出契约平台。
- 不改 Compose/Team 调度，不把内建角色做成 Plugin Agent，不把 `[models.roles]` 当第二入口。
- 不改真实用户配置，不覆盖用户现有修改，不为通过检查刷新其他任务日期。
- 发现需要改变公开行为、范围或生命周期时，回上游文档修订；不得在 Todo 执行中发明新设计。
